<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Monster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Monster" parent="Base.Theme.Monster" />
    <style name="bottom_bar" parent="Widget.Material3.BottomNavigationView">
        <!-- Fondo de la barra -->
        <item name="android:background">@color/white</item>

        <!-- Color del texto e íconos seleccionados -->
        <item name="itemTextColor">@color/bottom_bar_item_color</item>
        <item name="itemIconTint">@color/bottom_bar_item_color</item>

        <item name="itemRippleColor">@color/bottom_bar_ripple</item>
    </style>
</resources>